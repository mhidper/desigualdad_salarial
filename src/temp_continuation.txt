        ict_dist = df_combined.groupby(['year', 'ict']).size().unstack(fill_value=0)
        ict_dist.columns = ['Sectores_Tradicionales', 'Sectores_ICT']
        print("\\nDistribución por sectores ICT:")
        print(ict_dist)
        
        # Salarios medios por sector ICT
        ict_salaries = df_combined.groupby(['year', 'ict'])['salhora'].mean().unstack().round(2)
        ict_salaries.columns = ['Tradicionales', 'ICT']
        print("\\nSalarios por hora medios (ICT vs Tradicionales):")
        print(ict_salaries)

print("✅ Estadísticas descriptivas generadas")